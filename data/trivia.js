let f = require("./f.js");

let trivia = {
  tokens: [],
  responses: ["Success", "No results", "Invalid parameter", "Token not found", "Token empty"],
};

function update(dT) {
  
};
module.exports = function() {
  
  return trivia;
};